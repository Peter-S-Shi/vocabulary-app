# Desktop runtime dependencies for the native desktop UI (src/ui_desktop/)
# and desktop core services (backup, data tools import/export).
#
# Contains all third-party dependencies required to run the native desktop
# application without Streamlit.
#
# Selected per docs/design/M16_1_DESKTOP_ARCHITECTURE_CONTRACT.md.

PySide6>=6.11,<7
openpyxl>=3.1
